const state = {
  keepAlive: ["home-page"]
};

const getters = {
  getKeepAlives: state => state.keepAlive
};

const mutations = {

};

const actions = {

};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};